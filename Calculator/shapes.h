#pragma once
#include <iostream>
#include <vector>
#include <stdexcept>
#include <cmath>

class Shapes {
public:
    double area_circle(double radius);
    double area_triangle(double a, double b, double c);
};
